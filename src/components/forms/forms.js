import input from "./input.js"
import textarea from "./textarea.js"
import { radioWrapper, radio, radioGroup, radioButton} from "./radio.js"
import { checkboxWrapper, checkbox, checkboxToggle } from "./checkbox.js"
import { select } from "./select.js"

export default {
  input,
  textarea,
  radioWrapper, radio, radioGroup, radioButton,
  checkbox, checkboxToggle, checkboxWrapper,
  select
}